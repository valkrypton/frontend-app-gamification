import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  title: {
    id: 'gamification.dashboardPage.title',
    defaultMessage: 'Gamification Dashboard',
    description: 'Page heading for the gamification dashboard',
  },
});

export default messages;
